spring: #스프링 부트는 별도에 설정이 없으면 그냥 메모리 모드로 돌림 ㄷㄷ
#  datasource:
#    url: jdbc:h2:mem:test #메모리 JVM 안에서 실행
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create #엔티티 다 만들고 실행, 기본 : create-drop 마지막에 drop 쿼리를 만들어서 없애버림
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
